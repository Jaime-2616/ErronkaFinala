<UserControl x:Class="Cliente.Views.LobbyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Resources>
        <Color x:Key="AccentColor">#E0625E</Color>
        <Color x:Key="AccentDark">#C44F4B</Color>

        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="MutedBrush" Color="#8A8A8A"/>

        <DropShadowEffect x:Key="CardShadow"
                          Color="#44000000"
                          BlurRadius="18"
                          ShadowDepth="4"/>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="50"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="12"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IconButtonNoHover" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- UPDATED: leaderboard items show 3 aligned columns (# / name / points) -->
        <Style x:Key="LeaderboardListBox" TargetType="ListBox">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>

            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="Margin" Value="0,4"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Bd"
                                            CornerRadius="10"
                                            Background="White"
                                            BorderBrush="#EAEAEA"
                                            BorderThickness="1">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#10E0625E"/>
                                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter TargetName="Bd" Property="Opacity" Value="0.6"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>

            <!-- IMPORTANT: Each column binds to a real property now -->
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="48"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- # -->
                            <TextBlock Grid.Column="0"
                                       Text="{Binding Rank}"
                                       Foreground="{StaticResource MutedBrush}"
                                       FontSize="12"
                                       VerticalAlignment="Center"/>

                            <!-- Jugador -->
                            <TextBlock Grid.Column="1"
                                       Text="{Binding Username}"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Center"
                                       Margin="6,0,10,0"
                                       TextTrimming="CharacterEllipsis"/>

                            <!-- Puntos -->
                            <TextBlock Grid.Column="2"
                                       Text="{Binding Points}"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource AccentBrush}"
                                       VerticalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#F7F9FB" Offset="0"/>
                <GradientStop Color="#EEF2F5" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Ellipse Width="580"
                 Height="580"
                 Fill="#20E0625E"
                 HorizontalAlignment="Right"
                 VerticalAlignment="Top"
                 Margin="0,-180,-180,0"/>

        <Border Width="1300"
                Height="700"
                Padding="36"
                CornerRadius="16"
                Background="White"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Effect="{StaticResource CardShadow}">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="24"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- Puntos (izquierda) -->
                    <TextBlock x:Name="MyPointsText"
                               Grid.Column="0"
                               Text="Puntos: 0"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource MutedBrush}"
                               HorizontalAlignment="Left"
                               VerticalAlignment="Top"
                               Margin="0,14,0,0"/>

                    <!-- Historial (derecha, primero de los botones) -->
                    <Button x:Name="HistoryButton"
                            Grid.Column="1"
                            Width="120"
                            Height="36"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Margin="0,8,8,0"
                            Cursor="Hand"
                            Click="HistoryButton_Click"
                            Style="{StaticResource PrimaryButton}">
                        <TextBlock Text="Historial"
                                   Foreground="White"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Button>

                    <Button x:Name="PokedexButton"
                            Grid.Column="2"
                            Width="100"
                            Height="36"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Margin="0,8,8,0"
                            Cursor="Hand"
                            Click="PokedexButton_Click"
                            Style="{StaticResource PrimaryButton}">
                        <TextBlock Text="Taldea" Foreground="White" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>

                    <!-- Logout button -->
                    <Button x:Name="LogoutButton"
                            Grid.Column="3"
                            Width="140"
                            Height="36"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Margin="0,8,64,0"
                            Cursor="Hand"
                            Click="BtnLogout_Click"
                            Style="{StaticResource PrimaryButton}">
                        <TextBlock Text="Itxi saioa" Foreground="White" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>
                </Grid>

                <!-- Body -->
                <Grid Grid.Row="2" Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="400" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- Left panel (players) -->
                    <StackPanel Grid.Column="0" Margin="8,0,4,0">
                        <TextBlock Text="Konektatutako jokalariak"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource MutedBrush}"
                                   Margin="0,0,0,8"
                                   HorizontalAlignment="Center"/>
                        <Border Background="#FAFAFA"
                                CornerRadius="12"
                                Height="360"
                                BorderBrush="#E8E8E8"
                                BorderThickness="1"
                                Effect="{StaticResource CardShadow}">
                            <StackPanel Margin="12">
                                <ListBox x:Name="PlayersListBox"
                                         Margin="0,0,0,8"
                                         FontSize="14"/>
                                <Button x:Name="ChallengeButton"
                                        Content="Erronka"
                                        Height="36"
                                        HorizontalAlignment="Right"
                                        Width="100"
                                        Style="{StaticResource PrimaryButton}"
                                        Click="ChallengeButton_Click"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- Middle panel (Top 10) -->
                    <StackPanel Grid.Column="1" Margin="4,0,4,0">
                        <Border CornerRadius="14"
                                Background="White"
                                BorderBrush="#E8E8E8"
                                BorderThickness="1"
                                Height="360"
                                Effect="{StaticResource CardShadow}">
                            <Grid Margin="14">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- Header -->
                                <DockPanel Grid.Row="0" LastChildFill="True" Margin="0,0,0,10">
                                    <Border Width="34" Height="34"
                                            CornerRadius="10"
                                            Background="#10E0625E"
                                            DockPanel.Dock="Left"
                                            Margin="0,0,10,0">
                                        <TextBlock Text="&#xE734;"
                                                   FontFamily="Segoe MDL2 Assets"
                                                   Foreground="{StaticResource AccentBrush}"
                                                   FontSize="16"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"/>
                                    </Border>

                                    <StackPanel>
                                        <TextBlock Text="Top 10"
                                                   FontSize="16"
                                                   FontWeight="SemiBold"
                                                   Foreground="Black"/>
                                        <TextBlock Text="Jugadores con mas puntos"
                                                   FontSize="12"
                                                   Foreground="{StaticResource MutedBrush}"/>
                                    </StackPanel>
                                </DockPanel>

                                <!-- Column labels -->
                                <Grid Grid.Row="1" Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="48"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Text="#"
                                               Grid.Column="0"
                                               FontSize="12"
                                               Foreground="{StaticResource MutedBrush}"/>

                                    <TextBlock Text="Jugador"
                                               Grid.Column="1"
                                               FontSize="12"
                                               Foreground="{StaticResource MutedBrush}"/>

                                    <TextBlock Text="Puntos"
                                               Grid.Column="2"
                                               FontSize="12"
                                               Foreground="{StaticResource MutedBrush}"/>
                                </Grid>

                                <!-- List -->
                                <ListBox x:Name="Top5ListBox"
                                         Grid.Row="2"
                                         Style="{StaticResource LeaderboardListBox}"
                                         FontSize="14"/>
                            </Grid>
                        </Border>
                    </StackPanel>

                    <!-- Right panel (chat) -->
                    <StackPanel Grid.Column="2" Margin="4,0,8,0">
                        <TextBlock Text="Txat"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource MutedBrush}"
                                   Margin="0,0,0,8"
                                   HorizontalAlignment="Center"/>
                        <Border Background="#FAFAFA"
                                CornerRadius="12"
                                Height="360"
                                BorderBrush="#E8E8E8"
                                BorderThickness="1"
                                Effect="{StaticResource CardShadow}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Messages list -->
                                <ListBox x:Name="ChatListBox"
                                         Grid.Row="0"
                                         Margin="12"
                                         FontSize="14"/>

                                <!-- Input + Send -->
                                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="12">
                                    <TextBox x:Name="ChatTextBox" Height="36" VerticalAlignment="Center" MinWidth="240" KeyDown="ChatTextBox_KeyDown"/>
                                    <Button x:Name="SendButton" Content="Bidali" Width="100" Height="36" Margin="8,0,0,0" Click="SendButton_Click" Style="{StaticResource PrimaryButton}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>